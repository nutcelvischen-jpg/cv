
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Elvis Chen — AI Researcher</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&family=Geist+Mono:wght@300;400;500&display=swap" rel="stylesheet">

<!-- GSAP -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"></script>

<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  @font-face {
    font-family: 'PixelOperator';
    src: url('data:font/woff2;base64,') format('woff2');
    font-display: swap;
  }

  :root {
    --bg: #f5f0e8;
    --bg-alt: #ede6da;
    --surface: #faf7f2;
    --text: #1a1714;
    --text-secondary: #5c5650;
    --text-muted: #9c9488;
    --accent: #1a1714;
    --accent-dim: #e8e0d4;
    --accent2: #5c5650;
    --border: #d4cbc0;
    --radius: 0px;
    --max-width: 1100px;
    --pixel-size: 4px;
  }

  html { scroll-behavior: auto; background: var(--bg); }

  body {
    font-family: 'VT323', 'Courier New', monospace;
    background: var(--bg);
    color: var(--text);
    line-height: 1.6;
    -webkit-font-smoothing: none;
    overflow-x: hidden;
    font-size: 1.15rem;
  }

  /* ── Grid overlay ── */
  .grid-bg {
    position: fixed; inset: 0; z-index: 0; pointer-events: none; opacity: 0.06;
    background-image:
      linear-gradient(rgba(0,0,0,0.15) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,0,0,0.15) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  /* ── Typography ── */
  h1, h2 { font-family: 'Press Start 2P', 'Courier New', monospace; line-height: 1.4; }
  h1 { font-size: clamp(1.2rem, 3.5vw, 2rem); }
  h2 { font-size: clamp(0.8rem, 2vw, 1.1rem); margin-bottom: 2rem; }
  h3 {
    font-size: 0.6rem; font-family: 'Press Start 2P', monospace; text-transform: uppercase;
    letter-spacing: 0.1em; color: var(--accent); margin-bottom: 0.75rem;
  }

  p { color: var(--text-secondary); max-width: 65ch; font-size: 1.1rem; }

  a { color: var(--accent); text-decoration: none; }
  a:hover { color: var(--accent2); }

  /* ── Layout ── */
  .container { max-width: var(--max-width); margin: 0 auto; padding: 0 1.5rem; position: relative; z-index: 1; }

  /* ── Navigation ── */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    background: rgba(245,240,232,0.92); border-bottom: 2px solid var(--border);
    image-rendering: pixelated;
  }
  .nav-inner {
    display: flex; justify-content: space-between; align-items: center;
    height: 56px; max-width: var(--max-width); margin: 0 auto; padding: 0 1.5rem;
  }
  .nav-brand {
    font-family: 'Press Start 2P', monospace; font-size: 0.55rem; color: var(--accent);
    letter-spacing: 0;
  }
  .nav-links { display: flex; gap: 1.5rem; list-style: none; }
  .nav-links a {
    color: var(--text-secondary); font-size: 0.65rem;
    font-family: 'Press Start 2P', monospace; letter-spacing: 0;
    transition: color 0.15s;
  }
  .nav-links a:hover { color: var(--accent); }

  /* ── Hero ── */
  .hero {
    min-height: 100dvh; display: flex; flex-direction: column; justify-content: center;
    align-items: center; text-align: center; padding: 6rem 1.5rem 4rem;
    position: relative; z-index: 1;
  }

  /* ── PARTICLE A.I. LOGO ── */
  .ai-particle-wrap {
    position: relative;
    width: min(680px, 92vw);
    margin-bottom: 2.5rem;
  }
  #ai-particle-canvas {
    display: block;
    width: 100%;
    border: 2px solid var(--border);
    background: transparent;
    cursor: crosshair;
  }
  .ai-particle-controls {
    display: flex; gap: 0.75rem; margin-top: 0.75rem; justify-content: center;
  }
  .ai-btn {
    padding: 0.35rem 0.9rem;
    border: 2px solid var(--accent); background: transparent;
    color: var(--accent); font-family: 'Press Start 2P', monospace; font-size: 0.38rem;
    cursor: pointer; letter-spacing: 0.05em;
    transition: background 0.15s; image-rendering: pixelated;
  }
  .ai-btn:hover { background: rgba(0,0,0,0.06); }
  .ai-btn:active { transform: scale(0.96); }

  /* ── Hero text ── */
  .hero-name { font-family: 'Press Start 2P', monospace; font-size: clamp(0.9rem, 2.5vw, 1.3rem); margin: 1.5rem 0 1rem; color: var(--accent); }
  .hero-subtitle {
    font-family: 'VT323', monospace; font-size: clamp(1rem, 2vw, 1.3rem);
    color: var(--text-secondary); max-width: 600px; margin-bottom: 2rem;
  }
  .hero-cta {
    display: inline-flex; align-items: center; gap: 0.5rem;
    padding: 0.6rem 1.25rem; border: 2px solid var(--accent); color: var(--accent);
    font-family: 'Press Start 2P', monospace; font-size: 0.55rem;
    background: transparent; cursor: pointer;
    transition: background 0.15s; image-rendering: pixelated;
  }
  .hero-cta:hover { background: rgba(0,0,0,0.04); }

  /* ── Sections ── */
  section { padding: 5rem 0; position: relative; z-index: 1; }
  .section-header { margin-bottom: 3rem; }

  /* ── About ── */
  .about-grid { display: grid; grid-template-columns: 1fr 1.2fr; gap: 3rem; align-items: start; }
  .about-visual {
    position: relative; aspect-ratio: 1;
    border: 2px solid var(--accent); background: var(--surface);
    display: flex; align-items: center; justify-content: center; overflow: hidden;
    image-rendering: pixelated;
  }
  .about-visual canvas { position: absolute; inset: 0; }
  .about-visual-label { font-family: 'Press Start 2P', monospace; font-size: 1.2rem; color: var(--accent); position: relative; z-index: 1; }
  .about-text { display: flex; flex-direction: column; gap: 1rem; }
  .about-title { font-family: 'Press Start 2P', monospace; font-size: 0.55rem; color: var(--accent); line-height: 1.8; }
  .about-role { color: var(--text); font-weight: 400; margin-bottom: 0.5rem; font-size: 1.2rem; }
  .about-highlights { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 1rem; }
  .about-tag {
    background: var(--accent-dim); color: var(--accent); border: 1px solid rgba(0,255,136,0.3);
    padding: 0.2rem 0.6rem; font-family: 'Press Start 2P', monospace; font-size: 0.4rem;
    image-rendering: pixelated;
  }

  /* ── Skills ── */
  .skills-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1.5rem; }
  .skill-card {
    background: var(--surface); border: 2px solid var(--border); padding: 1.5rem;
    transition: border-color 0.15s; image-rendering: pixelated;
  }
  .skill-card:hover { border-color: var(--accent); }
  .skill-card h3 { font-size: 0.55rem; color: var(--accent); margin-bottom: 1rem; letter-spacing: 0.05em; }
  .skill-card .tags { display: flex; flex-wrap: wrap; gap: 0.4rem; }

  /* ── Portfolio ── */
  .portfolio-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1rem; }
  .portfolio-card {
    background: var(--surface); border: 2px solid var(--border); overflow: hidden;
    transition: border-color 0.15s; cursor: pointer; display: block; text-decoration: none;
    image-rendering: pixelated;
  }
  .portfolio-card:hover { border-color: var(--accent); }
  .portfolio-visual {
    height: 120px; background: var(--bg-alt); position: relative; overflow: hidden;
    display: flex; align-items: center; justify-content: center;
  }
  .portfolio-visual canvas { position: absolute; inset: 0; }
  .portfolio-visual-label {
    font-family: 'Press Start 2P', monospace; font-size: 0.7rem; color: var(--accent); position: relative; z-index: 1;
  }
  .portfolio-body { padding: 1rem 1.25rem; }
  .portfolio-body h3 {
    font-family: 'Press Start 2P', monospace; font-size: 0.5rem; font-weight: 400; color: var(--accent);
    margin-bottom: 0.5rem; text-transform: none; letter-spacing: 0.02em; line-height: 1.6;
  }
  .portfolio-body p { font-size: 0.95rem; color: var(--text-muted); }

  /* ── Contact ── */
  .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; }
  .contact-info { display: flex; flex-direction: column; gap: 1.5rem; }
  .contact-item { display: flex; flex-direction: column; gap: 0.2rem; }
  .contact-item h3 { font-size: 0.5rem; margin-bottom: 0; }
  .contact-item span, .contact-item a { color: var(--text-secondary); font-size: 1rem; }
  .contact-form { display: flex; flex-direction: column; gap: 1rem; }
  .contact-form input, .contact-form textarea {
    padding: 0.6rem 0.75rem; border: 2px solid var(--border);
    background: var(--surface); color: var(--text);
    font-family: 'VT323', monospace; font-size: 1rem;
    transition: border-color 0.15s; image-rendering: pixelated;
  }
  .contact-form input:focus, .contact-form textarea:focus { outline: none; border-color: var(--accent); }
  .contact-form textarea { min-height: 100px; resize: vertical; }
  .contact-form button {
    padding: 0.5rem 1.25rem; background: transparent; border: 2px solid var(--accent);
    color: var(--accent); font-family: 'Press Start 2P', monospace; font-size: 0.5rem;
    cursor: pointer; width: fit-content; image-rendering: pixelated;
  }
  .contact-form button:hover { background: rgba(0,0,0,0.04); }

  /* ── Footer ── */
  footer {
    padding: 2.5rem 0; border-top: 2px solid var(--border);
    color: var(--text-muted); font-size: 0.7rem; font-family: 'Press Start 2P', monospace;
    position: relative; z-index: 1; text-align: center;
  }
  .footer-inner { max-width: var(--max-width); margin: 0 auto; padding: 0 1.5rem; }

  /* ── Scanline effect ── */
  .scanlines {
    position: fixed; inset: 0; z-index: 999; pointer-events: none; opacity: 0.03;
    background: repeating-linear-gradient(0deg, transparent, transparent 2px, rgba(0,0,0,0.03) 2px, rgba(0,0,0,0.03) 4px);
  }

  @media (max-width: 768px) {
    .about-grid { grid-template-columns: 1fr; }
    .contact-grid { grid-template-columns: 1fr; }
    .nav-links { gap: 0.8rem; }
    .nav-links a { font-size: 0.45rem; }
  }

  @media (prefers-reduced-motion: reduce) {
    .scanlines { display: none; }
  }
</style>
</head>
<body>

<div class="grid-bg"></div>
<div class="scanlines"></div>

<!-- Navigation -->
<nav>
  <div class="nav-inner">
    <a href="#" class="nav-brand">ELVIS.CHEN</a>
    <ul class="nav-links">
      <li><a href="#about">ABOUT</a></li>
      <li><a href="#skills">SKILLS</a></li>
      <li><a href="#portfolio">RESEARCH</a></li>
      <li><a href="#contact">CONTACT</a></li>
    </ul>
  </div>
</nav>

<!-- Hero -->
<section class="hero" id="hero">
  <!-- PARTICLE A.I. CANVAS -->
  <div class="ai-particle-wrap">
    <canvas id="ai-particle-canvas"></canvas>
    <div class="ai-particle-controls">
      <button class="ai-btn" onclick="toggleDeconstruct()" id="ai-btn-text">DECONSTRUCT()</button>
      <button class="ai-btn" onclick="triggerAssembleEffect()">SCATTER()</button>
    </div>
  </div>

  <div class="hero-name" id="hero-name">ELVIS CHEN</div>
  <p class="hero-subtitle" id="hero-subtitle">
    * ASSISTANT PROFESSOR<br>
    * AI RESEARCHER<br>
    * ENVIRONMENTAL ECONOMICS<br>
    * DATA-DRIVEN POLICY
  </p>
  <button class="hero-cta" id="hero-cta" onclick="document.getElementById('about').scrollIntoView()">
    &gt; EXPLORE_
  </button>
</section>

<!-- About -->
<section id="about">
  <div class="container">
    <div class="section-header">
      <h3>[ ABOUT ]</h3>
      <h2>ABOUT</h2>
    </div>
    <div class="about-grid">
      <div class="about-visual">
        <canvas id="about-canvas"></canvas>
        <span class="about-visual-label">EC</span>
      </div>
      <div class="about-text">
        <div class="about-title">PH.D. NYCU / 2020</div>
        <div class="about-role">Assistant Professor, Dept. of International Business, NTCUST</div>
        <p>I am passionate about extracting insights from data to solve complex problems. My recent work on simulating the EU's circular economy using deep learning was funded by the National Science and Technology Council.</p>
        <p>Visiting scholar at North Carolina State University. I bring humor and a passion for sports to every team I join.</p>
        <div class="about-highlights">
          <span class="about-tag">PH.D. NYCU</span>
          <span class="about-tag">NSTC FUNDED</span>
          <span class="about-tag">AI+ECON</span>
          <span class="about-tag">DEEP LEARNING</span>
          <span class="about-tag">NCSU VISITING</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Skills -->
<section id="skills">
  <div class="container">
    <div class="section-header">
      <h3>[ STACK ]</h3>
      <h2>TOOLCHAIN</h2>
    </div>
    <div class="skills-grid">
      <div class="skill-card">
        <h3>DEV & TOOLS</h3>
        <div class="tags">
          <span class="about-tag">PYTHON</span><span class="about-tag">R</span><span class="about-tag">SAS</span>
          <span class="about-tag">SQL</span><span class="about-tag">C++</span>
          <span class="about-tag">HTML</span><span class="about-tag">CSS</span><span class="about-tag">JS</span>
        </div>
      </div>
      <div class="skill-card">
        <h3>DATA ANALYSIS</h3>
        <div class="tags">
          <span class="about-tag">TABLEAU</span><span class="about-tag">SPSS</span>
          <span class="about-tag">EVIEWS</span><span class="about-tag">AMOS</span><span class="about-tag">SMARTPLS</span>
        </div>
      </div>
      <div class="skill-card">
        <h3>DESIGN</h3>
        <div class="tags">
          <span class="about-tag">GIMP</span><span class="about-tag">ILLUSTRATOR</span><span class="about-tag">PHOTOSHOP</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Portfolio -->
<section id="portfolio">
  <div class="container">
    <div class="section-header">
      <h3>[ RESEARCH ]</h3>
      <h2>SELECTED WORK</h2>
    </div>
    <div class="portfolio-grid">
      <a href="#" class="portfolio-card"><div class="portfolio-visual"><canvas class="port-canvas"></canvas><span class="portfolio-visual-label">[P1]</span></div><div class="portfolio-body"><h3>PUBLICATIONS</h3><p>Peer-reviewed articles in environmental economics and AI applications.</p></div></a>
      <a href="#" class="portfolio-card"><div class="portfolio-visual"><canvas class="port-canvas"></canvas><span class="portfolio-visual-label">[T1]</span></div><div class="portfolio-body"><h3>TALKS</h3><p>Invited presentations across Asia and the United States.</p></div></a>
      <a href="#" class="portfolio-card"><div class="portfolio-visual"><canvas class="port-canvas"></canvas><span class="portfolio-visual-label">[R1]</span></div><div class="portfolio-body"><h3>PROJECTS</h3><p>NSTC-funded circular economy + deep learning.</p></div></a>
      <a href="#" class="portfolio-card"><div class="portfolio-visual"><canvas class="port-canvas"></canvas><span class="portfolio-visual-label">[A1]</span></div><div class="portfolio-body"><h3>AWARDS</h3><p>Academic distinctions and competitive research grants.</p></div></a>
      <a href="#" class="portfolio-card"><div class="portfolio-visual"><canvas class="port-canvas"></canvas><span class="portfolio-visual-label">[T2]</span></div><div class="portfolio-body"><h3>TEACHING</h3><p>AI, data analysis, international business course materials.</p></div></a>
      <a href="#" class="portfolio-card"><div class="portfolio-visual"><canvas class="port-canvas"></canvas><span class="portfolio-visual-label">[G1]</span></div><div class="portfolio-body"><h3>AI GAME</h3><p>ChatGPT-powered educational game project.</p></div></a>
    </div>
  </div>
</section>

<!-- Contact -->
<section id="contact">
  <div class="container">
    <div class="section-header">
      <h3>[ CONTACT ]</h3>
      <h2>GET IN TOUCH</h2>
    </div>
    <div class="contact-grid">
      <div class="contact-info">
        <div class="contact-item"><h3>EMAIL</h3><a href="mailto:elvischen@nutc.edu.tw">elvischen@nutc.edu.tw</a></div>
        <div class="contact-item"><h3>PHONE</h3><span>+886-4-22196120</span></div>
        <div class="contact-item"><h3>ADDRESS</h3><span>Rm 3705, 7F Zhongzheng Bldg<br>129 Sec 3 Sanmin Rd, North Dist<br>Taichung 404, Taiwan</span></div>
      </div>
      <form class="contact-form" onsubmit="event.preventDefault();alert('MESSAGE SENT!');">
        <input type="text" placeholder="NAME" required>
        <input type="email" placeholder="EMAIL" required>
        <textarea placeholder="YOUR MESSAGE..." required></textarea>
        <button type="submit">SEND_MSG()</button>
      </form>
    </div>
  </div>
</section>

<footer>
  <div class="footer-inner">
    ELVIS.CHEN [AT] NUTC.EDU.TW &nbsp;//&nbsp; SYSTEM READY. &copy; 2026
  </div>
</footer>

<!-- ── SCRIPTS ── -->
<script>
gsap.registerPlugin(ScrollTrigger);

// ══════════════════════════════════════════
// A.I. PARTICLE LOGO — gestalt pixel art
// ══════════════════════════════════════════
(function() {
  const settings = {
    cols: 115, rows: 83,
    forceRadius: 70,
    deconstruct: false
  };

  const canvas = document.getElementById('ai-particle-canvas');
  const ctx = canvas.getContext('2d');
  const offscreen = document.createElement('canvas');
  const offCtx = offscreen.getContext('2d');

  let particles = [], canvasWidth = 0, canvasHeight = 0, pSize = 10;
  const mouse = { x: -9999, y: -9999, radius: settings.forceRadius };

  class PixelParticle {
    constructor(c, r, targetX, targetY, size, type) {
      this.col = c; this.row = r;
      this.ox = targetX; this.oy = targetY;
      this.x = targetX + (Math.random() - 0.5) * 120;
      this.y = targetY + (Math.random() - 0.5) * 120;
      this.size = size; this.type = type;
      this.vx = 0; this.vy = 0;
      this.friction = 0.85; this.spring = 0.12;
      this.alpha = 1.0; this.targetAlpha = 1.0;
    }
    update(deconstructActive) {
      let targetX = this.ox, targetY = this.oy;
      this.targetAlpha = 1.0;
      if (deconstructActive) {
        if (this.type === 'neg-boy') {
          targetX = this.ox + 330 * (canvasWidth / 900);
        } else if (this.type === 'I') {
          this.targetAlpha = 0.15;
        }
      }
      const dx = this.x - mouse.x, dy = this.y - mouse.y;
      const dist = Math.sqrt(dx*dx + dy*dy);
      let fx = 0, fy = 0;
      if (dist < mouse.radius) {
        const force = (mouse.radius - dist) / mouse.radius;
        const angle = Math.atan2(dy, dx);
        fx = Math.cos(angle) * force * 16;
        fy = Math.sin(angle) * force * 16;
      }
      const ax = (targetX - this.x) * this.spring;
      const ay = (targetY - this.y) * this.spring;
      this.vx = (this.vx + ax + fx) * this.friction;
      this.vy = (this.vy + ay + fy) * this.friction;
      this.x += this.vx; this.y += this.vy;
      this.alpha += (this.targetAlpha - this.alpha) * 0.1;
    }
    draw(renderCtx) {
      let color;
      if (this.type === 'neg-boy' && settings.deconstruct) {
        // Warm amber accent when deconstructed (matches site palette)
        color = `rgba(90, 80, 60, ${this.alpha * 0.9})`;
      } else {
        // Steel-sepia gradient: site's dark text tone with subtle warmth
        const val = Math.floor(30 + Math.sin(this.ox * 0.012) * 18);
        const r = val + 2, g = val, b = val - 6;
        color = `rgba(${r}, ${g}, ${b}, ${this.alpha})`;
      }
      renderCtx.fillStyle = color;
      const s = this.size * 0.78;
      const x = Math.round(this.x - s/2), y = Math.round(this.y - s/2);
      const radius = Math.max(1, s * 0.28);
      if (radius > 1.5) {
        renderCtx.beginPath();
        renderCtx.roundRect(x, y, Math.ceil(s), Math.ceil(s), radius);
        renderCtx.fill();
      } else {
        renderCtx.fillRect(x, y, Math.ceil(s), Math.ceil(s));
      }
    }
  }

  function buildPixelGrid() {
    const cols = settings.cols, rows = settings.rows;
    offscreen.width = cols; offscreen.height = rows;
    offCtx.clearRect(0, 0, cols, rows);
    offCtx.save();
    offCtx.scale(cols / 900, rows / 650);
    const boyPath = new Path2D("M 45,540 C 42,530 40,510 41,490 C 42,460 46,430 48,400 C 50,370 50,340 48,310 C 46,280 41,250 36,220 C 34,210 31,190 32,175 C 33,160 40,150 43,130 C 44,120 41,110 36,100 C 31,90 27,75 29,60 C 31,42 45,25 62,20 C 80,15 96,28 100,45 C 102,55 98,65 101,75 C 104,85 114,92 118,100 C 122,108 118,115 112,120 C 108,123 110,128 113,132 C 116,136 122,140 118,148 C 115,152 108,153 105,158 C 102,163 103,169 101,175 C 98,181 92,188 90,198 C 88,208 89,228 88,248 C 87,278 91,308 93,338 C 95,368 96,398 95,428 C 94,458 91,488 89,518 C 88,530 92,535 100,538 C 108,541 115,541 118,543 C 120,545 115,545 45,540 Z");
    // A body
    offCtx.fillStyle = '#FF0000';
    offCtx.beginPath();
    offCtx.moveTo(100,650); offCtx.lineTo(250,80); offCtx.lineTo(350,80);
    offCtx.lineTo(500,650); offCtx.lineTo(400,650); offCtx.lineTo(350,470);
    offCtx.lineTo(215,470); offCtx.lineTo(160,650);
    offCtx.closePath(); offCtx.fill();
    // Cut out boy silhouette + inner triangle
    offCtx.globalCompositeOperation = 'destination-out';
    offCtx.save(); offCtx.translate(250,100); offCtx.fill(boyPath); offCtx.restore();
    offCtx.beginPath();
    offCtx.moveTo(175,650); offCtx.lineTo(245,470); offCtx.lineTo(290,650);
    offCtx.closePath(); offCtx.fill();
    offCtx.globalCompositeOperation = 'source-over';
    // Negative-space boy (green channel)
    offCtx.fillStyle = '#00FF00';
    offCtx.save(); offCtx.translate(250,100); offCtx.fill(boyPath); offCtx.restore();
    // I character (blue channel)
    offCtx.fillStyle = '#0000FF';
    offCtx.save(); offCtx.translate(580,100); offCtx.fill(boyPath); offCtx.restore();
    // Dots
    offCtx.fillStyle = '#FFFF00';
    offCtx.beginPath(); offCtx.arc(545,625,23,0,Math.PI*2); offCtx.fill();
    offCtx.beginPath(); offCtx.arc(810,625,23,0,Math.PI*2); offCtx.fill();
    offCtx.restore();

    const imgData = offCtx.getImageData(0,0,cols,rows);
    const data = imgData.data;
    particles = [];
    pSize = canvasWidth / cols;
    for (let r = 0; r < rows; r++) {
      for (let c = 0; c < cols; c++) {
        const idx = (r * cols + c) * 4;
        const R = data[idx], G = data[idx+1], B = data[idx+2], A = data[idx+3];
        if (A > 120) {
          let type = null;
          if (R > 200 && G < 50 && B < 50) type = 'A';
          else if (G > 200 && R < 50 && B < 50) type = 'neg-boy';
          else if (B > 200 && R < 50 && G < 50) type = 'I';
          else if (R > 200 && G > 200 && B < 50) type = 'dots';
          if (type) {
            const px = c * pSize + pSize/2, py = r * pSize + pSize/2;
            particles.push(new PixelParticle(c, r, px, py, pSize, type));
          }
        }
      }
    }
  }

  function animate() {
    ctx.clearRect(0, 0, canvasWidth, canvasHeight);
    // Subtle grid — beige palette
    ctx.save();
    ctx.strokeStyle = 'rgba(180,170,155,0.18)';
    ctx.lineWidth = 0.5;
    for (let x = 0; x < canvasWidth; x += pSize) {
      ctx.beginPath(); ctx.moveTo(x,0); ctx.lineTo(x,canvasHeight); ctx.stroke();
    }
    for (let y = 0; y < canvasHeight; y += pSize) {
      ctx.beginPath(); ctx.moveTo(0,y); ctx.lineTo(canvasWidth,y); ctx.stroke();
    }
    ctx.restore();
    for (let i = 0; i < particles.length; i++) {
      const p = particles[i];
      p.update(settings.deconstruct);
      if (p.type === 'neg-boy' && !settings.deconstruct) continue;
      p.draw(ctx);
    }
    requestAnimationFrame(animate);
  }

  function resizeCanvas() {
    const rect = canvas.getBoundingClientRect();
    canvasWidth = rect.width || canvas.offsetWidth;
    canvasHeight = Math.round(canvasWidth * (650 / 900));
    canvas.width = canvasWidth;
    canvas.height = canvasHeight;
    buildPixelGrid();
  }

  window.toggleDeconstruct = function() {
    settings.deconstruct = !settings.deconstruct;
    document.getElementById('ai-btn-text').textContent =
      settings.deconstruct ? 'ASSEMBLE()' : 'DECONSTRUCT()';
  };
  window.triggerAssembleEffect = function() {
    particles.forEach(p => {
      p.x = p.ox + (Math.random()-0.5) * canvasWidth;
      p.y = p.oy + (Math.random()-0.5) * canvasHeight;
    });
  };

  canvas.addEventListener('mousemove', e => {
    const rect = canvas.getBoundingClientRect();
    mouse.x = e.clientX - rect.left;
    mouse.y = e.clientY - rect.top;
  });
  canvas.addEventListener('mouseleave', () => { mouse.x = -9999; mouse.y = -9999; });
  canvas.addEventListener('touchmove', e => {
    if (e.touches.length > 0) {
      const rect = canvas.getBoundingClientRect();
      mouse.x = e.touches[0].clientX - rect.left;
      mouse.y = e.touches[0].clientY - rect.top;
    }
  });
  canvas.addEventListener('touchend', () => { mouse.x = -9999; mouse.y = -9999; });
  window.addEventListener('resize', resizeCanvas);

  window.addEventListener('load', () => {
    resizeCanvas();
    animate();
  });
})();

// ══════════════════════════════════════════
// NEURAL NET CANVASES
// ══════════════════════════════════════════
function createNeuralNet(canvasId) {
  const canvas = document.getElementById(canvasId);
  if (!canvas) return;
  const ctx = canvas.getContext('2d');
  const parent = canvas.parentElement;
  ctx.imageSmoothingEnabled = false;

  function resize() {
    canvas.width  = parent.offsetWidth;
    canvas.height = parent.offsetHeight;
  }
  resize();
  new ResizeObserver(resize).observe(parent);

  const nodes = [];
  for (let i = 0; i < 25; i++) {
    nodes.push({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      vx: (Math.random() - 0.5) * 0.4,
      vy: (Math.random() - 0.5) * 0.4,
      r: 2,
      pulse: Math.random() * Math.PI * 2
    });
  }

  function draw() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    // Connections
    for (let i = 0; i < nodes.length; i++) {
      for (let j = i + 1; j < nodes.length; j++) {
        const dx = nodes[i].x - nodes[j].x;
        const dy = nodes[i].y - nodes[j].y;
        const dist = Math.hypot(dx, dy);
        if (dist < 120) {
          ctx.beginPath(); ctx.moveTo(nodes[i].x, nodes[i].y); ctx.lineTo(nodes[j].x, nodes[j].y);
          ctx.strokeStyle = `rgba(26,23,20,${0.08 * (1 - dist/120)})`; ctx.lineWidth = 1; ctx.stroke();
        }
      }
    }
    // Nodes
    nodes.forEach(n => {
      n.x += n.vx; n.y += n.vy; n.pulse += 0.02;
      if (n.x < 0 || n.x > canvas.width) n.vx *= -1;
      if (n.y < 0 || n.y > canvas.height) n.vy *= -1;
      const a = 0.3 + Math.sin(n.pulse) * 0.15;
      ctx.fillStyle = `rgba(26,23,20,${Math.max(0.1, a)})`;
      ctx.fillRect(Math.round(n.x - n.r/2), Math.round(n.y - n.r/2), n.r, n.r);
    });
    requestAnimationFrame(draw);
  }
  draw();
}

createNeuralNet('about-canvas');
document.querySelectorAll('.port-canvas').forEach(c => {
  c.parentElement.__canvas = c;
  // For portfolio cards, use simpler approach
  const ctx2 = c.getContext('2d');
  ctx2.imageSmoothingEnabled = false;
  const parent2 = c.parentElement;

  function resize2() {
    c.width  = parent2.offsetWidth;
    c.height = parent2.offsetHeight;
  }
  resize2();
  new ResizeObserver(resize2).observe(parent2);

  const nodes2 = [];
  for (let i = 0; i < 10; i++) {
    nodes2.push({
      x: Math.random() * 400, y: Math.random() * 150,
      vx: (Math.random() - 0.5) * 0.3, vy: (Math.random() - 0.5) * 0.3, r: 2
    });
  }

  function draw2() {
    ctx2.clearRect(0, 0, c.width, c.height);
    for (let i = 0; i < nodes2.length; i++) {
      for (let j = i + 1; j < nodes2.length; j++) {
        const dx = nodes2[i].x - nodes2[j].x, dy = nodes2[i].y - nodes2[j].y;
        const dist = Math.hypot(dx, dy);
        if (dist < 80) {
          ctx2.beginPath(); ctx2.moveTo(nodes2[i].x, nodes2[i].y); ctx2.lineTo(nodes2[j].x, nodes2[j].y);
          ctx2.strokeStyle = `rgba(26,23,20,${0.06 * (1 - dist/80)})`; ctx2.lineWidth = 1; ctx2.stroke();
        }
      }
    }
    nodes2.forEach(n => {
      n.x += n.vx; n.y += n.vy;
      if (n.x < 2 || n.x > (c.width - 2)) n.vx *= -1;
      if (n.y < 2 || n.y > (c.height - 2)) n.vy *= -1;
      ctx2.fillStyle = 'rgba(26,23,20,0.25)';
      ctx2.fillRect(Math.round(n.x - 1), Math.round(n.y - 1), 2, 2);
    });
    requestAnimationFrame(draw2);
  }
  draw2();
});

// ══════════════════════════════════════════
// GSAP SCROLL ANIMATIONS
// ══════════════════════════════════════════

// Hero entrance sequence
window.addEventListener('load', () => {
  const tl = gsap.timeline();
  tl.from('.ai-particle-wrap', { opacity: 0, scale: 0.96, duration: 1.0, ease: 'power3.out' }, 0.3)
    .from('#hero-name', { opacity: 0, y: 30, duration: 0.8, ease: 'power3.out' }, 0.6)
    .from('#hero-subtitle', { opacity: 0, y: 20, duration: 0.8, ease: 'power3.out' }, '-=0.4')
    .from('#hero-cta', { opacity: 0, scale: 0.9, duration: 0.5, ease: 'back.out(1.5)' }, '-=0.4')
    .from('.ai-particle-controls', { opacity: 0, y: 8, duration: 0.4, ease: 'power2.out' }, '-=0.3');
});

// Section content reveals
gsap.utils.toArray('section .container > *').forEach(el => {
  gsap.from(el, {
    scrollTrigger: { trigger: el, start: 'top 85%', toggleActions: 'play none none none' },
    opacity: 0, y: 24, duration: 0.7, ease: 'power2.out'
  });
});

// Skill cards stagger
gsap.from('.skill-card', {
  scrollTrigger: { trigger: '#skills', start: 'top 80%' },
  opacity: 0, y: 40, duration: 0.5, stagger: 0.1, ease: 'power2.out'
});

// Portfolio cards stagger
gsap.from('.portfolio-card', {
  scrollTrigger: { trigger: '#portfolio', start: 'top 80%' },
  opacity: 0, y: 40, duration: 0.5, stagger: 0.08, ease: 'power2.out'
});
</script>

</body>
</html>
